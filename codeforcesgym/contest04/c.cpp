#include <iostream> 
using namespace std; 

int main() {
    int t; 
    cin >> t; 
    while (t--) {
        int64_t n; 
        cin >> n; 
        int64_t heaps[n];
        for(int i = 0; i < n; i++) {
            int64_t h; 
            cin >> h; 
            heaps[i] = h; 
        }
    }
}