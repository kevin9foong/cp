#include<iostream> 
#include<cmath> 
using namespace std; 

int main() {
    int64_t n, k, p; 
    cin >> n >> k >> p; 

    for (int N = 0; N < n; N++) {

    }
}

bool canTalk(int a, int b, int k, int n, int dist[], bool **dp) {
    if (abs(dist[a] - dist[b]) <= k) {
        dp[a][b] = true; 
    } else { 
        for (int i = 0; )
    }
}